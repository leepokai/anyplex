node_modules/
dist/
coverage/
.env
*.log
.claude/
.agents/

examples/.resume.json
examples/.agents.json

# Local mirror of the vendors' documentation (their copyright); rebuild with `pnpm docs:vendor`.
vendor-docs/

# VitePress site output and cache (pnpm docs:build / docs:dev)
docs/.vitepress/cache/
docs/.vitepress/dist/

# Remotion launch video renders (pnpm --dir video run render)
video/out/
# Regenerate with `python3 video/scripts/music.py`; the AAC copy is committed.
video/public/music.wav
